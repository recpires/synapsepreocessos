git-push.bat
*.tmp
*.log
.DS_Store
Thumbs.db

# Estado de runtime de ferramentas — não é código do projeto
.claude-flow/
.claude/settings.local.json

# Backups locais do painel (contêm dados financeiros)
painel-interno/backups/

# Arquivos vazios criados por acidente de redirecionamento no shell.
# Nomes com parêntese, crase, ponto ou dígito solto não são código deste projeto.
painel-interno/[0-9]
painel-interno/*.status
painel-interno/b.margem

# Testes de interface geram nada, mas o loader do tsx deixa cache
.tsx-cache/
